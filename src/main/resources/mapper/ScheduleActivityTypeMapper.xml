<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.life.yolo.mapper.ScheduleActivityTypeMapper">

    <insert id="batchUpdateSortOrder">
        INSERT INTO schedule_activity_type (id, user_id, sort, type_code, type_name, color, enable_flag)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{userId}, #{item.sortOrder}, '', '', '', 1)
        </foreach>
        ON DUPLICATE KEY UPDATE
            sort = IF(user_id = VALUES(user_id), VALUES(sort), sort),
            update_time = NOW()
    </insert>

</mapper>