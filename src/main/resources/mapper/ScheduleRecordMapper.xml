<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.life.yolo.mapper.ScheduleRecordMapper">

    <insert id="batchSaveRecords" parameterType="java.util.List">
        INSERT INTO schedule_record (
            user_id, biz_date, time_slot, record_type, activity_type, create_time, update_time
        ) VALUES
        <foreach collection="list" item="record" separator=",">
            (
                #{record.userId},
                #{record.bizDate},
                #{record.timeSlot},
                #{record.recordType},
                #{record.activityType},
                NOW(),
                NOW()
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            activity_type = VALUES(activity_type),
            update_time = NOW()
    </insert>

</mapper>