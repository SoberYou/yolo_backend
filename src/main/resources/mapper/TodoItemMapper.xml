<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.life.yolo.mapper.TodoItemMapper">

    <insert id="batchUpdateSortOrder">
        INSERT INTO todo_item (id, user_id, sort_order, date_type, start_date, end_date, content)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{userId}, #{item.sortOrder}, '', '', '', '')
        </foreach>
        ON DUPLICATE KEY UPDATE
            sort_order = IF(user_id = VALUES(user_id), VALUES(sort_order), sort_order),
            update_time = NOW()
    </insert>

</mapper>